# 32-byte key, hex or base64 encoded. Example hex (64 chars):
# SPOTIFY_CRED_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
SPOTIFY_CRED_ENCRYPTION_KEY=

# Optional override for data storage directory
# SPOTIFY_DATA_DIR=/absolute/path/to/data

# Optional base URL for Spotify OAuth redirect (use if localhost is not allowed)
# SPOTIFY_REDIRECT_BASE=http://127.0.0.1:3000

# Optional client credentials for server-side artist lookups (Now Playing)
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
